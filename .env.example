# Optional override for GroupMe OAuth configuration used by the GroupUs login page.
# Copy this file to .env.local only if you want to override the built-in defaults.

VITE_GROUPME_OAUTH_CLIENT_ID=
VITE_GROUPME_OAUTH_CALLBACK_URL=http://127.0.0.1:53682/oauth/callback
